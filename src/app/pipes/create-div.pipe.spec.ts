import { CreateDivPipe } from './create-div.pipe';

describe('CreateDivPipe', () => {
  it('create an instance', () => {
    const pipe = new CreateDivPipe();
    expect(pipe).toBeTruthy();
  });
});
